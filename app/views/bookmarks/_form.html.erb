<%= form_for(current_user.bookmarks.find(params[:id])) do |f| %>
  <% if current_user.bookmarks.find(params[:id]).errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(current_user.bookmarks.find(params[:id]).errors.count, "error") %> prohibited this bookmark from being saved:</h2>

      <ul>
      <% current_user.bookmarks.find(params[:id]).errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
